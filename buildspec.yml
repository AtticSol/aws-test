version: 0.1

phases:
  install:
    runtime-versions:
      java: corretto8

  build:
    commands:
      - mvn clean install --quiet

artifacts:
  discard-paths: yes  # yamllint disable-line rule:truthy
  files:
    - target/*
    - scripts/*
    - appspec.yml
